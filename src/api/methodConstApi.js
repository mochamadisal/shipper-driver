import {API_URL} from '~/constants/variable';

// EX : [BASEURL (String), METHOD (String), ENDPOINT (String), ADD ON URL (Boolean)]

// User
export const GetUsersAPI = [API_URL, 'get', '/api', true];
